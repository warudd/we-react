Hook
Vite คืออะไร

useEffect อยากรู้ว่ามันใช้ในกรณี user ทำอะไรกับหน้าเว็บ ถึงจะเข้าฟังก์ชันนี้ ? 
useContext เข้าใจแค่ว่าเป็นการโยน Props ข้าม Components ยังไม่เข้าใจหลักการเขียน
useReducer เข้าใจว่าเป็นการสร้าง components แยกขึ้นมา และให้ components อื่น import ไปใช้ ยังไม่เข้าใจหลักการเขียน